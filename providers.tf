terraform {
  required_providers {
    #aws = {
    #  source  = "hashicorp/aws"
    #}
    random = {
      source = "hashicorp/random"
    }
    local = {
      source = "hashicorp/local"
    }
  }
}

#provider "aws" {
#  region  = "us-west-2"
#}
